[tool.ruff]
line-length = 79
target-version = "py310"
src = ["financas"]

[tool.ruff.lint]
# E/W = pycodestyle, F = pyflakes, I = ordenação de imports (isort).
select = ["E", "W", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["models", "repositories", "services", "cli", "config"]
